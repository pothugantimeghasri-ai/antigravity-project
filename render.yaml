services:
  - type: web
    name: ai-data-science-mentor-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main_grounded:app --host 0.0.0.0 --port $PORT
