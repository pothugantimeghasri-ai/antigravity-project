services:
  - type: web
    name: ai-data-science-mentor-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main_grounded:app --host 0.0.0.0 --port $PORT
